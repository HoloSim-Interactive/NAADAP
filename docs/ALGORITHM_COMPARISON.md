# Alternative-Approach Comparison (CORE-260)

<!--
Produced by the CORE-260 analysis run (issue #10). Feeds DELIV-970's
algorithm documentation (issue #12) rather than being that document
itself — DELIV-970 requires algorithm/dependency/deployment
documentation to exist as discrete, individually locatable submission
artifacts, and this file is the CORE-260-specific piece of that:
the documented, rejected comparison substantiating the non-LLM core's
selection.
-->

## What this is

Per `docs/PROJECT_DEFINITION.md` Scope: "Alongside the no-LLM-in-core-
clustering approach, demonstrate at least one alternative strategy
(e.g. retrieval/RAG- or LLM-assisted clustering) and document its
shortcomings through rigorous testing." This is that comparison — an
**offline analysis run**, executed during development/validation, not
a second production path. It is never invoked from `Naadap.Cli` or
wired into the OUT-440 bundle (`src/Naadap.Alternative` is a separate
assembly, never referenced by `Naadap.Core` or `Naadap.Cli`; see
`docs/SDD.md`'s block diagram).

## The two approaches compared

1. **Non-LLM core** — `Naadap.Core.TfIdfCosineClusteringComponent`
   (CORE-200). TF-IDF term weighting plus a single global cosine-
   similarity threshold (0.35); two documents join the same cluster if
   they meet that threshold directly or transitively (single-link).
2. **Alternative** — `Naadap.Alternative.RetrievalAugmentedClusteringComponent`
   (CORE-260). Same TF-IDF vector space, but a retrieval-based
   (RAG-style) connectivity rule instead of a global threshold: each
   document retrieves its top-2 most-similar other documents (the
   literal retrieval step of a RAG pipeline), and two documents join
   the same cluster only if they retrieve each other (a mutual k-NN
   edge) — locally adaptive, rather than one fixed cutoff applied
   everywhere.

Neither approach calls a live LLM/microservice in this analysis: the
requirement's own phrasing offers "LLM-assisted **or**
retrieval/RAG-based" as alternatives, and this implements the
retrieval half directly. This development environment has no
accredited USN-approved model endpoint to call, so rather than
fabricate a stand-in for one, the comparison reports the alternative's
real LLM token cost as what it actually is for this approach: zero.
That is a genuine, reportable data point, not a placeholder — see
"Findings" below.

## Methodology

Both approaches are run, by the exact same harness
(`Naadap.Alternative.ApproachRunner`), against the same input: the N=20
reference set (`tests/fixtures/reference-20/`, see
`tests/fixtures/README.md`) and its derived `ground-truth.json`. Both
are scored by the exact same `Naadap.Output.VehicleRecommender`
(ranking) and `Naadap.Output.MetricCalculator` (precision@5) the
production pipeline itself uses — not a reimplementation that could
drift from OUT-420's real metric definition
(`docs/VALIDATION_METHODOLOGY.md`).

**Reproduce this run:**

```
dotnet run --project src/Naadap.Alternative -- tests/fixtures/reference-20 docs/ALGORITHM_COMPARISON.results.md
```

(first argument: input directory, defaults to `tests/fixtures/reference-20`
if omitted; second argument: optional path to also write the report to
a file, in addition to stdout).

## Results (run 2026-09-17, after gate G4)

Re-run after the G4 fix to `VehicleRecommender.ComputeCohesion`: a
single-document cluster now scores 0.0 rather than 1.0, so singletons no
longer outrank multi-document clusters (see the G4 note below).

Generated by `Naadap.Alternative` against `tests/fixtures/reference-20` (20 document(s) ingested, 1 skipped).

| Approach | Clusters | Candidates | Clustering time | Total time | precision@5 | Correct/Total | LLM tokens used |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Non-LLM core (TF-IDF cosine, global-threshold single-link) | 14 | 14 | 222.2 ms | 402.2 ms | 0.60 | 3/5 | 0 |
| Alternative (retrieval-based, mutual k-NN, TF-IDF) | 10 | 10 | 229.5 ms | 412.7 ms | 0.80 | 4/5 | 0 |

## Metric definitions

- **Non-LLM core (TF-IDF cosine, global-threshold single-link):** precision@5: of the top 5 ranked candidate(s), 3 matched a distinct ground-truth vehicle by majority vote of their contributing documents (each real vehicle counted at most once, for the highest-ranked matching candidate). See docs/VALIDATION_METHODOLOGY.md for the full definition.
- **Alternative (retrieval-based, mutual k-NN, TF-IDF):** precision@5: of the top 5 ranked candidate(s), 4 matched a distinct ground-truth vehicle by majority vote of their contributing documents (each real vehicle counted at most once, for the highest-ranked matching candidate). See docs/VALIDATION_METHODOLOGY.md for the full definition.

(Wall-clock times are illustrative of relative cost on this development
machine, not a formal CORE-220 runtime measurement; both approaches
process the 20-document set in well under a second.)

## Results before G4 (run 2026-09-03, singleton score 1.0) — superseded, kept for the record

# CORE-260 alternative-approach comparison

Generated by `Naadap.Alternative` against `tests/fixtures/reference-20` (20 document(s) ingested, 1 skipped — `ground-truth.json` itself, an unsupported file type for ingestion, exactly as it is in every other pipeline run against this directory).

| Approach | Clusters | Candidates | Clustering time | Total time | precision@5 | Correct/Total | LLM tokens used |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Non-LLM core (TF-IDF cosine, global-threshold single-link) | 14 | 14 | 148.3 ms | 320.2 ms | 0.60 | 3/5 | 0 |
| Alternative (retrieval-based, mutual k-NN, TF-IDF) | 10 | 10 | 131.2 ms | 298.8 ms | 0.40 | 2/5 | 0 |

(Wall-clock times are illustrative of relative cost on this development
machine, not a formal CORE-220 runtime measurement — both approaches
process the same 20-document set in well under a second either way, so
neither is a runtime concern relative to the 30-minute budget. This is
consistent with CORE-260 being an accuracy/robustness comparison, not
a performance one.)

## Findings — revised 2026-09-17 after gate G4

The 2026-09-03 findings below this section were written when singleton
clusters scored 1.0 and therefore filled the top five of both rankings.
That convention was wrong (gate G4, `docs/design/vehicle-recommendation-pipeline.md`):
a single document carries no cross-document evidence that a requirement is
shared. With singletons scored 0.0 the picture changes and the honest record
is this:

- **Accuracy on precision@5 against the four-vehicle ground truth:** core
  0.60 (3 of 5), alternative 0.80 (4 of 5). With four ground-truth vehicles
  the ceiling for precision@5 is 0.80, so the alternative reaches the
  ceiling and the core misses one vehicle. Before G4 the alternative scored
  0.40; its earlier deficit was an artifact of singleton ranking, not of
  its clustering.
- **What the difference is.** The core's top five are its three
  multi-document clusters (majority votes SeaPort-NxG, GSA MAS, and SBRAC)
  plus two SeaPort-NxG singletons, which add nothing because SeaPort-NxG is
  already counted. NAVFAC MACC/JOC never reaches the core's top five as a
  majority. One candidate on a 20-document set with four vehicles is the
  entire gap.
- **What the difference is not.** It is not an LLM-versus-no-LLM result:
  both approaches use TF-IDF and call no model (0 tokens). It is not a
  statistically meaningful result: one candidate, one corpus, one ground
  truth derived by inspection. And precision@5 is queued for replacement
  by MRR and Recall@5 labeled "agreement with historical practice"
  (SEMP §2.1.4), which will be measured against FPDS holdouts, not this
  fixture.
- **Robustness and simplicity, unchanged.** The core has one tuned scalar
  (the 0.35 threshold, derivation in `TfIdfCosineClusteringComponent`).
  The alternative's retrieval depth `K` is untuned. Single-link at a global
  threshold yields transitively connected clusters whose members share a
  measurable pairwise similarity, which is the evidence a consolidation
  record needs; mutual k-NN's locally adaptive rule is harder to explain
  to a contracting officer as a threshold.

**Conclusion, revised.** The non-LLM core selection stands for Increment 1
on the robustness and explainability axis, and CORE-260's purpose (an
implemented, tested, documented alternative) is met. The accuracy claim
made on 2026-09-03 is withdrawn: on this fixture and this metric the
alternative scores higher by one candidate. The core-versus-alternative
choice is reopened at gate G6 as a Class I decision (SEMP §3.2.10), with
this result on the record and with the Increment 2 metric, not precision@5,
as the basis for any change.

## Findings as written 2026-09-03 (superseded; retained unedited for the record)

*Why the non-LLM core was selected, as argued before G4:*


- **Accuracy:** the non-LLM core scored precision@5 0.60 (3/5) versus
  the alternative's 0.40 (2/5) on the same reference set. The core's
  single global similarity threshold, tuned specifically against the
  CORE-200 synthetic fixture (see
  `Naadap.Core.TfIdfCosineClusteringComponent`'s threshold-derivation
  remarks), forms fewer, larger, more cohesive clusters (14 clusters
  vs. the alternative's 10) that map more consistently onto the
  reference set's 4 known real vehicles.
- **The alternative's documented shortcoming:** mutual k-NN's
  stricter, locally-adaptive connectivity rule is more conservative
  about linking documents — a genuine property of retrieval-based
  clustering (a document only joins a cluster if it's a top-2 match
  for its partner in *both* directions), but on this corpus that
  conservatism produced a coarser, less accurate partition against the
  specific four-vehicle ground truth this challenge scores against,
  rather than a more precise one. This is the "shortcoming through
  rigorous testing" the client's scope note (`docs/PROJECT_DEFINITION.md`)
  asked to be documented rather than omitted.
- **Cost:** identical LLM token cost (0) for both, since neither calls
  an LLM in this analysis — a RAG-style retrieval approach's
  distinguishing cost advantage over an LLM-assisted approach is that
  it need not call a model at all for the clustering decision itself,
  which is also why CORE-250's separate `NAADAP_LLM_ENABLED` optional
  step exists independently of this comparison (summarization only,
  never used to make the clustering decision).
- **Robustness/complexity:** the core's algorithm requires only a
  single tuned scalar (the similarity threshold); the alternative
  requires a retrieval depth parameter (`K`) which was not
  independently tuned against this reference set the way the core's
  threshold was (see CORE-200's derivation) — a second, unexplored
  degree of freedom that could plausibly close some of the accuracy
  gap with further tuning, but was out of scope for this comparison to
  chase given CORE-260's purpose is substantiating the already-made
  core selection, not optimizing a second production candidate.

**Conclusion:** this run substantiates the client's non-LLM,
TF-IDF-cosine core selection (`docs/PROJECT_DEFINITION.md`,
[CONFIRMED, client 2026-09-03]) on both the accuracy axis the
challenge's judging criteria weight most heavily (50% validation
accuracy) and the robustness/simplicity axis it also rewards (50%
robustness, including LLM cost) — the alternative is demonstrably
implemented, tested, and its shortcoming (lower accuracy from stricter,
locally-adaptive connectivity on this specific ground truth) is
documented here per the client's explicit instruction, rather than
omitted.
