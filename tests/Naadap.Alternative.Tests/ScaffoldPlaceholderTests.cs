namespace Naadap.Alternative.Tests;

/// <summary>
/// Placeholder so the test project builds and `dotnet test` has something
/// to run before CORE-260's real behavior lands in Naadap.Alternative. Replace with real
/// tests as that RTVM item is implemented.
/// </summary>
public class ScaffoldPlaceholderTests
{
    [Fact]
    public void PlaceholderPassesUntilRealTestsExist()
    {
        Assert.True(true);
    }
}
